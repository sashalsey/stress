import firedrake as fd  # type: ignore
help(fd.PointEvaluator)

# conditional(condition, true_value, false_value)
# gt(left, right)
#     A boolean expression (left > right) 
# ge(left, right)
#     A boolean expression (left >= right)